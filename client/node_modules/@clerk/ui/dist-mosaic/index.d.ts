import { TabsProps } from "@clerk/headless/tabs";
import { ComponentProps } from "@clerk/headless/utils";
import * as stylex from "@stylexjs/stylex";
import * as React$1 from "react";
import React, { ReactNode } from "react";
import { Dialog as Dialog$1, DialogFocusTarget, DialogHandle, DialogOpenChangeDetails, DialogProps as DialogProps$1 } from "@clerk/headless/dialog";
import { Menu as Menu$1, MenuItemProps as MenuItemProps$1, MenuPopupProps, MenuPortalProps, MenuProps, MenuSeparatorProps } from "@clerk/headless/menu";
import { Popover as Popover$1, PopoverProps } from "@clerk/headless/popover";

//#region src/mosaic/props.d.ts
/**
 * The native props for a tag, minus the non-standard HTML `color` attribute. That
 * attribute is typed `string`, so leaving it in widens any component that exposes
 * `color` as a variant union. Use for a component that has no `render`.
 */
type MosaicElementProps<Tag extends keyof React.JSX.IntrinsicElements> = Omit<React.ComponentPropsWithRef<Tag>, 'color'>;
/**
 * The base props every Mosaic component accepts: the native props for its default
 * tag, plus the `render` escape hatch that swaps the rendered element.
 *
 * Mosaic's name for the headless part contract, which already drops `color` and
 * hands `render` callbacks tag-agnostic props. Aliased rather than rebuilt so the
 * two layers cannot drift, and so `@clerk/ui` consumers have a name to import.
 */
type MosaicComponentProps<Tag extends keyof React.JSX.IntrinsicElements> = ComponentProps<Tag>;
type VariantValue = string | number | boolean | null | undefined;
/**
 * The stable class + variant reflection for a slot.
 *
 * Returns `{ className: 'cl-<slot>', 'data-<axis>': value, … }`. Each variant
 * axis becomes a `data-<axis>` attribute a consumer can scope overrides on.
 * Booleans reflect only when `true` (as an empty-string presence attr, mirroring
 * how the Emotion engine emitted `data-cl-disabled`); `null`/`undefined`/`false`
 * are skipped. The className is only the stable slot class — variant values live
 * in the data-attrs, not extra classes, to avoid colliding with consumer classes.
 */
declare function themeProps(slot: string, variants?: Record<string, VariantValue>): {
  className: string;
} & Record<string, string>;
type PropsObject = {
  className?: string;
  style?: React.CSSProperties;
  [key: string]: unknown;
};
/**
 * Fuse `themeProps(...)`, `stylex.props(...)`, and the consumer's incoming
 * `className`/`style` into one spreadable object. Positional, mirroring astryx's
 * helper so the consumer's `className` and `style` can be passed raw:
 *
 *   mergeStyleProps(themeProps('button', { variant }), stylex.props(...), className, style)
 *
 * The trailing pair disambiguates by type — a string is a `className`, an object
 * is a `style` — which is why `style` is accepted directly rather than wrapped.
 * Order is deliberate: stable class + data-attrs, then StyleX atoms, then the
 * consumer's `className`/`style` last so they win.
 *
 * Distinct from `@clerk/headless`'s `mergeProps`: this only fuses styling output
 * (className/style) and does not chain event handlers.
 */
declare function mergeStyleProps(first: PropsObject, second?: PropsObject | string, classNameOrStyle?: string | React.CSSProperties, style?: React.CSSProperties): PropsObject;
//#endregion
//#region src/mosaic/icons/registry.d.ts
/** A default icon glyph — receives `svg` props (sizing/color flow in via `className`) and forwards a ref. */
type IconComponent = React$1.ForwardRefExoticComponent<React$1.ComponentPropsWithoutRef<'svg'> & React$1.RefAttributes<SVGSVGElement>>;
/** Runtime name → glyph map. `Icon`'s `name` prop is typed from these keys. */
declare const iconRegistry: {
  'alert-circle': IconComponent;
  'arrow-right-top': IconComponent;
  'chevron-right': IconComponent;
  'chevron-left': IconComponent;
  'chevron-down': IconComponent;
  'chevron-up': IconComponent;
  'chevron-up-down': IconComponent;
  check: IconComponent;
  close: IconComponent;
  code: IconComponent;
  'credit-card': IconComponent;
  ellipsis: IconComponent;
  pen: IconComponent;
  plus: IconComponent;
  search: IconComponent;
  'shield-check': IconComponent;
  'log-out': IconComponent;
  cog: IconComponent;
  'device-laptop': IconComponent;
  'device-phone': IconComponent;
  'security-authenticator': IconComponent;
  'security-lock-square': IconComponent;
  'security-passkey': IconComponent;
  'security-phone': IconComponent;
  users: IconComponent;
  'user-circle': IconComponent;
};
type IconName = keyof typeof iconRegistry;
//#endregion
//#region src/mosaic/profile-page.d.ts
interface ProfilePageItem {
  value: string;
  label: string;
  icon: IconName;
}
interface ProfilePageRootProps extends Omit<MosaicComponentProps<'div'>, 'children'> {
  value: string;
  onValueChange?: (value: string) => void;
  orientation?: TabsProps['orientation'];
  activationMode?: TabsProps['activationMode'];
  children: React.ReactNode;
}
interface ProfilePageSidebarProps extends Omit<MosaicComponentProps<'aside'>, 'children'> {
  items: readonly ProfilePageItem[];
  navigationLabel: string;
  renderBranding?: boolean;
}
interface ProfilePageContentProps extends Omit<MosaicComponentProps<'main'>, 'children'> {
  children: React.ReactNode;
}
interface ProfilePagePanelProps extends MosaicComponentProps<'div'> {
  value: string;
}
declare const ProfilePage: {
  Root: React.ForwardRefExoticComponent<Omit<ProfilePageRootProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Sidebar: React.ForwardRefExoticComponent<Omit<ProfilePageSidebarProps, "ref"> & React.RefAttributes<HTMLElement>>;
  Content: React.ForwardRefExoticComponent<Omit<ProfilePageContentProps, "ref"> & React.RefAttributes<HTMLElement>>;
  Panel: React.ForwardRefExoticComponent<Omit<ProfilePagePanelProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
};
//#endregion
//#region src/mosaic/components/alert-dialog/confirm-handle.d.ts
/** What a confirmation asks. Delivered to `<AlertDialog.Confirm>` as the dialog's payload. */
interface ConfirmOptions {
  title: ReactNode;
  description: ReactNode;
  /** Label on the confirming button. @default 'Confirm' */
  actionLabel?: ReactNode;
  /** Label on the declining button. @default 'Cancel' */
  cancelLabel?: ReactNode;
  /** Colours the action as destructive, for a confirmation that discards or deletes. */
  destructive?: boolean;
}
/**
 * Links a `show()` call to the `<AlertDialog.Confirm>` that answers it. Create with
 * {@link createConfirmHandle}; `show` is the whole public surface.
 */
interface ConfirmHandle {
  /**
   * Opens the confirmation and resolves with the user's answer: `true` for the action, `false`
   * for cancel or any dismissal.
   *
   * Calling it while a confirmation is already showing returns the IN-FLIGHT promise rather than
   * opening a second one — repeated Escapes against a guarded dialog would otherwise stack
   * confirmations, one per keypress. The options of the later call are ignored, since the
   * question on screen is already the one being answered.
   *
   * The `<AlertDialog.Confirm>` must be MOUNTED when this is called. It is what opens, and a
   * `dialog.open()` with no root attached is a no-op. Since the confirmation belongs inside the
   * dialog it guards, that means asking only from inside that dialog while it is open. Calling it
   * with nothing mounted resolves `false` and warns in development; a confirmation that unmounts
   * with a question in flight answers `false` too, rather than hanging.
   */
  show(options: ConfirmOptions): Promise<boolean>;
  /** The dialog handle `<AlertDialog.Confirm>` mounts against. @internal */
  readonly dialog: DialogHandle<ConfirmOptions>;
  /**
   * Resolves the in-flight promise, if any. Idempotent per question: the second call for the same
   * `show()` is a no-op, which is what lets the action settle `true` and then close through the
   * ordinary path without the close settling `false` on top of it. @internal
   */
  settle(confirmed: boolean): void;
}
/**
 * Creates a {@link ConfirmHandle}: an awaitable confirmation, in the shape of a promise rather
 * than a pair of state variables and a callback.
 *
 * ```ts
 * const confirm = createConfirmHandle();
 * if (await confirm.show({ title: 'Discard changes?', description: '…' })) {
 *   discard();
 * }
 * ```
 *
 * The dialog itself is still rendered as JSX — `<AlertDialog.Confirm handle={confirm} />` — and
 * where it is rendered matters: it belongs inside the dialog it guards, so the two are in the same
 * floating tree and escape ordering, the stacking styles and the refcounted scroll lock all apply.
 * A confirmation mounted app-globally would be a sibling of the dialog rather than a child of it,
 * and every one of those would break.
 *
 * Create one per guarded dialog, at module scope or in a `useMemo` — never inside the render body
 * without one, since a new handle each render would orphan the promise a `show()` is waiting on.
 */
declare function createConfirmHandle(): ConfirmHandle;
//#endregion
//#region src/mosaic/components/dialog/dialog.styles.d.ts
declare const sizes: Readonly<{
  readonly prompt: Readonly<{
    readonly '--_cl-stack-veil': stylex.StyleXClassNameFor<"--_cl-stack-veil", 0 | 0.4>;
    readonly padding: stylex.StyleXClassNameFor<"padding", string>;
    readonly alignSelf: stylex.StyleXClassNameFor<"alignSelf", "end" | null>;
    readonly maxWidth: stylex.StyleXClassNameFor<"maxWidth", "none" | "23.75rem">;
  }>;
  readonly card: Readonly<{
    readonly padding: stylex.StyleXClassNameFor<"padding", null>;
    readonly gap: stylex.StyleXClassNameFor<"gap", null>;
    readonly backgroundColor: stylex.StyleXClassNameFor<"backgroundColor", null>;
    readonly boxShadow: stylex.StyleXClassNameFor<"boxShadow", null>;
    readonly maxWidth: stylex.StyleXClassNameFor<"maxWidth", "25rem">;
  }>;
  readonly panel: Readonly<{
    readonly padding: stylex.StyleXClassNameFor<"padding", string>;
    readonly overflow: stylex.StyleXClassNameFor<"overflow", "clip">;
    readonly alignSelf: stylex.StyleXClassNameFor<"alignSelf", "stretch">;
    readonly maxWidth: stylex.StyleXClassNameFor<"maxWidth", "94rem">;
  }>;
}>;
//#endregion
//#region src/mosaic/components/dialog/dialog.d.ts
/** Width of the dialog surface, and for `panel` its height too. */
type DialogSize = keyof typeof sizes;
interface DialogRootProps<Payload = unknown> extends DialogProps$1<Payload> {
  /** Width, and for `panel` also height, of the dialog surface. @default 'prompt' */
  size?: DialogSize;
}
/**
 * The headless parts type their props (and the `render` callback's argument) against
 * the raw tag props, which carry the non-standard HTML `color` attribute typed
 * `string`. Re-typing them through `MosaicComponentProps` drops it, so a `render`
 * callback can spread straight into a Mosaic component whose own `color` is a narrow
 * variant union.
 */
type DialogTriggerProps<Payload = unknown> = MosaicComponentProps<'button'> & {
  /**
   * Connects this trigger to a root rendered elsewhere in the tree. Create with
   * `Dialog.createHandle()` and pass the same handle to the `Dialog.Root`.
   */
  handle?: DialogHandle<Payload>;
  /**
   * Delivered to the root when this trigger opens it, for per-trigger content: the root's
   * children-as-function receives it as `{ payload }`.
   */
  payload?: Payload;
};
type DialogCloseProps = MosaicComponentProps<'button'>;
/** `id` is owned by the primitive, which wires it to the popup's `aria-labelledby`. */
type DialogTitleProps = Omit<MosaicComponentProps<'h2'>, 'id'>;
/** `id` is owned by the primitive, which wires it to the popup's `aria-describedby`. */
type DialogDescriptionProps = Omit<MosaicComponentProps<'p'>, 'id'>;
interface DialogCloseButtonProps extends MosaicComponentProps<'button'> {
  /**
   * Names the button for assistive technology. Defaults to English; pass a localized string
   * once one is available — no other change is needed when localization lands.
   */
  'aria-label'?: string;
}
type DialogBackdropProps = MosaicComponentProps<'div'>;
interface DialogViewportProps extends MosaicComponentProps<'div'> {
  /** When true, locks body scroll while the dialog is open. @default true */
  lockScroll?: boolean;
}
type DialogPopupProps = MosaicComponentProps<'div'> & {
  /** Where focus moves when the dialog opens. Default: the first tabbable element inside it. */initialFocus?: DialogFocusTarget; /** Where focus returns when the dialog closes. Default: the trigger. */
  finalFocus?: DialogFocusTarget;
};
interface DialogProps extends Pick<DialogProps$1, 'open' | 'defaultOpen' | 'onOpenChange' | 'modal' | 'closedBy'> {
  /**
   * Renders the button that opens the dialog. Omit for dialogs driven entirely by `open` —
   * opened from a menu item, a route, or a state machine — where there is no trigger to render.
   */
  trigger?: MosaicComponentProps<'button'>['render'];
  children: ReactNode | ((ctx: {
    close: () => void;
  }) => ReactNode);
  /** Width, and for `panel` also height, of the dialog surface. @default 'prompt' */
  size?: DialogSize;
}
/**
 * Mosaic `Dialog` — a modal surface built on the `@clerk/headless` dialog primitive.
 * Flattens the required nesting (Root, Portal, Backdrop, Viewport, Popup) into one
 * component and hands `children` a `close` callback through a render prop.
 *
 * Each styled part spreads `themeProps` + `stylex.props` through `mergeStyleProps`, so
 * it carries the public `.cl-<slot>` class and StyleX atoms while the headless part
 * keeps its focus management, scroll lock, and ARIA wiring. Drop to the compound parts
 * (`Dialog.Root` and friends) for layouts this wrapper does not cover.
 */
declare function Dialog({
  trigger,
  children,
  size,
  open,
  defaultOpen,
  onOpenChange,
  modal,
  closedBy
}: DialogProps): import("react/jsx-runtime").JSX.Element;
declare namespace Dialog {
  var Root: <Payload = unknown>({
    size,
    children,
    ...rest
  }: DialogRootProps<Payload>) => import("react/jsx-runtime").JSX.Element;
  var Trigger: <Payload = unknown>(props: DialogTriggerProps<Payload> & {
    ref?: React.Ref<HTMLButtonElement>;
  }) => React.ReactElement;
  var createHandle: typeof Dialog$1.createHandle;
  var Portal: typeof Dialog$1.Portal;
  var Backdrop: React.ForwardRefExoticComponent<Omit<DialogBackdropProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  var Viewport: React.ForwardRefExoticComponent<Omit<DialogViewportProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  var Popup: React.ForwardRefExoticComponent<Omit<DialogPopupProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  var Title: React.ForwardRefExoticComponent<Omit<DialogTitleProps, "ref"> & React.RefAttributes<HTMLHeadingElement>>;
  var Description: React.ForwardRefExoticComponent<Omit<DialogDescriptionProps, "ref"> & React.RefAttributes<HTMLParagraphElement>>;
  var Close: React.ForwardRefExoticComponent<Omit<DialogCloseProps, "ref"> & React.RefAttributes<HTMLButtonElement>>;
  var CloseButton: React.ForwardRefExoticComponent<Omit<DialogCloseButtonProps, "ref"> & React.RefAttributes<HTMLButtonElement>>;
}
//#endregion
//#region src/mosaic/components/alert-dialog/alert-dialog.d.ts
/**
 * An alert dialog is a `Dialog` with three decisions already made, so the props that would make
 * them are not offered:
 *
 * - `role` is `alertdialog`, which is the whole point — assistive technology announces it as an
 *   interruption rather than as a surface the user navigated to;
 * - `closedBy` is `closerequest`, so an outside press cannot dismiss it. A dialog asking a
 *   question it needs an answer to must not be answerable by clicking next to it. Escape still
 *   closes, which is not negotiable either: it is the keyboard's equivalent of the cancel button,
 *   and the cancel button is always present here;
 * - `size` is `prompt`, the size that means "asks one thing and returns".
 */
type AlertDialogRootProps<Payload = unknown> = Omit<DialogRootProps<Payload>, 'closedBy' | 'role' | 'size'>;
type AlertDialogTriggerProps<Payload = unknown> = DialogTriggerProps<Payload>;
type AlertDialogBackdropProps = DialogBackdropProps;
type AlertDialogViewportProps = DialogViewportProps;
type AlertDialogPopupProps = Omit<DialogPopupProps, 'role'>;
type AlertDialogTitleProps = DialogTitleProps;
type AlertDialogDescriptionProps = DialogDescriptionProps;
type AlertDialogCloseProps = DialogCloseProps;
type AlertDialogActionsProps = MosaicComponentProps<'div'>;
interface AlertDialogProps extends Pick<AlertDialogRootProps, 'open' | 'defaultOpen' | 'onOpenChange' | 'modal'>,
/**
 * Focus, forwarded to the popup. `finalFocus` earns its place on the wrapper rather than only
 * on the part: an alert is usually raised by something that happened rather than by a trigger,
 * and with no trigger there is nothing for focus to return to when it closes. Answering
 * "keep editing" should put the caret back in the field the question was about.
 */
Pick<AlertDialogPopupProps, 'finalFocus' | 'initialFocus'> {
  /**
   * Renders the button that opens the alert. Omit for alerts driven entirely by `open` — the
   * common case, since an alert is usually raised by something that already happened rather than
   * by a button that exists to raise it.
   */
  trigger?: MosaicComponentProps<'button'>['render'];
  children: ReactNode | ((ctx: {
    close: () => void;
  }) => ReactNode);
}
/**
 * Mosaic `AlertDialog` — a `Dialog` that interrupts to ask for a decision, and waits for one.
 *
 * Reach for it when continuing depends on the answer: confirming something destructive, or
 * warning that leaving loses work. Anything the user can simply read and dismiss is a `Dialog`.
 *
 * Composed from the same parts, so everything true of `Dialog` is true here — the surface, the
 * motion, the stacking over another dialog, the scroll lock. What differs is what it announces
 * itself as, that an outside press does not dismiss it, and that it carries a `Title`, a
 * `Description`, and an `Actions` row rather than arbitrary content. Both are checked in
 * development; neither is enforceable in the type system, since parts arrive as children.
 *
 * Drop to the compound parts (`AlertDialog.Root` and friends) for layouts this wrapper does not
 * cover.
 *
 * @example
 * <AlertDialog
 *   open={open}
 *   onOpenChange={setOpen}
 *   trigger={props => <Button {...props} color='negative'>Delete</Button>}
 * >
 *   <AlertDialog.Title>Delete this key?</AlertDialog.Title>
 *   <AlertDialog.Description>Applications using it will stop working immediately.</AlertDialog.Description>
 *   <AlertDialog.Actions>
 *     <AlertDialog.Close render={<Button variant='outline' />}>Cancel</AlertDialog.Close>
 *     <Button color='negative' onClick={remove}>Delete key</Button>
 *   </AlertDialog.Actions>
 * </AlertDialog>
 */
declare function AlertDialog({
  trigger,
  children,
  open,
  defaultOpen,
  onOpenChange,
  modal,
  initialFocus,
  finalFocus
}: AlertDialogProps): import("react/jsx-runtime").JSX.Element;
declare namespace AlertDialog {
  var Root: <Payload = unknown>({
    children,
    ...rest
  }: AlertDialogRootProps<Payload>) => import("react/jsx-runtime").JSX.Element;
  var Trigger: <Payload = unknown>(props: DialogTriggerProps<Payload> & {
    ref?: React.Ref<HTMLButtonElement>;
  }) => React.ReactElement;
  var createHandle;
  var Portal;
  var Backdrop: React.ForwardRefExoticComponent<Omit<DialogBackdropProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  var Viewport: React.ForwardRefExoticComponent<Omit<DialogViewportProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  var Popup: React.ForwardRefExoticComponent<Omit<AlertDialogPopupProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  var Title: React.ForwardRefExoticComponent<Omit<DialogTitleProps, "ref"> & React.RefAttributes<HTMLHeadingElement>>;
  var Description: React.ForwardRefExoticComponent<Omit<DialogDescriptionProps, "ref"> & React.RefAttributes<HTMLParagraphElement>>;
  var Close: React.ForwardRefExoticComponent<Omit<DialogCloseProps, "ref"> & React.RefAttributes<HTMLButtonElement>>;
  var Actions: React.ForwardRefExoticComponent<Omit<AlertDialogActionsProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  var Confirm: ({
    handle,
    finalFocus
  }: AlertDialogConfirmProps) => import("react/jsx-runtime").JSX.Element;
  var createConfirmHandle: typeof createConfirmHandle;
}
interface AlertDialogConfirmProps {
  /** Shared with the `show()` call, or with `useConfirmedClose`, that raises this confirmation. */
  handle: ConfirmHandle;
  /**
   * Where focus goes when the confirmation closes. Worth passing: the confirmation has no trigger,
   * so by default there is nothing for focus to return to. Point it at the field the question was
   * about and declining puts the caret back in it.
   */
  finalFocus?: DialogFocusTarget;
}
//#endregion
//#region src/mosaic/components/alert-dialog/use-confirmed-close.d.ts
interface UseConfirmedCloseOptions {
  /** The handle shared with the `<AlertDialog.Confirm>` rendered inside the guarded dialog. */
  handle: ConfirmHandle;
  /**
   * Whether closing needs confirming, evaluated at the moment of each close request — typically
   * "is this form dirty". Return `false` and the close commits immediately, with no interruption.
   */
  when: () => boolean;
  /** Commits the open state. The guarded dialog's own `onOpenChange`, usually a `setOpen`. */
  onOpenChange: (open: boolean, details: DialogOpenChangeDetails) => void;
  /** What to ask when `when()` returns true. */
  confirm: ConfirmOptions;
}
/**
 * Guards a dialog's close behind a confirmation, returning the `onOpenChange` to hand it.
 *
 * ```tsx
 * const onOpenChange = useConfirmedClose({
 *   handle: confirm,
 *   when: () => value !== '',
 *   onOpenChange: setOpen,
 *   confirm: { title: 'Discard changes?', description: '…', actionLabel: 'Discard' },
 * });
 *
 * <Dialog open={open} onOpenChange={onOpenChange} closedBy='closerequest'>
 *   …
 *   <AlertDialog.Confirm handle={confirm} />
 * </Dialog>
 * ```
 *
 * **The dialog must be controlled.** A veto is the absence of a commit, and an uncontrolled dialog
 * has already committed internally by the time `onOpenChange` runs — there would be nothing left to
 * decline.
 *
 * **What it covers is every close the dialog itself owns**: Escape, an outside press where
 * `closedBy` allows one, `Dialog.CloseButton`, `Dialog.Close`, and the `close` the `Dialog` wrapper
 * hands its children. All of them funnel through `onOpenChange`, so one branch here answers them
 * all. What it cannot cover is a button wired to your own `setOpen(false)` — that never reaches the
 * dialog, so it bypasses the question silently. Route those through `Dialog.Close` instead.
 */
declare function useConfirmedClose({
  handle,
  when,
  onOpenChange,
  confirm
}: UseConfirmedCloseOptions): (open: boolean, details: DialogOpenChangeDetails) => void;
//#endregion
//#region src/mosaic/components/avatar/avatar.d.ts
interface AvatarProps extends MosaicComponentProps<'span'> {
  shape?: 'circle' | 'square';
  size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl' | 'fit';
}
type AvatarImageProps = React.ComponentPropsWithRef<'img'>;
interface AvatarFallbackProps extends React.ComponentPropsWithRef<'span'> {
  /** Wait this many ms before showing the fallback, to avoid a flash on fast connections. */
  delayMs?: number;
}
type AvatarIconProps = React.ComponentPropsWithRef<'span'>;
/**
 * Compound avatar. `Avatar.Root` positions and sizes the box; `Avatar.Image` renders
 * once its source loads; `Avatar.Fallback` holds the space until then, as a blank
 * placeholder that pulses only while an image is actually on its way; `Avatar.Icon`
 * adds an optional corner affordance.
 */
declare const Avatar: {
  Root: React.ForwardRefExoticComponent<Omit<AvatarProps, "ref"> & React.RefAttributes<HTMLSpanElement>>;
  Image: React.ForwardRefExoticComponent<Omit<Omit<React.DetailedHTMLProps<React.ImgHTMLAttributes<HTMLImageElement>, HTMLImageElement>, "ref"> & {
    ref?: ((instance: HTMLImageElement | null) => void | React.DO_NOT_USE_OR_YOU_WILL_BE_FIRED_CALLBACK_REF_RETURN_VALUES[keyof React.DO_NOT_USE_OR_YOU_WILL_BE_FIRED_CALLBACK_REF_RETURN_VALUES]) | React.RefObject<HTMLImageElement> | null | undefined;
  }, "ref"> & React.RefAttributes<HTMLImageElement>>;
  Fallback: React.ForwardRefExoticComponent<Omit<AvatarFallbackProps, "ref"> & React.RefAttributes<HTMLSpanElement>>;
  Icon: React.ForwardRefExoticComponent<Omit<Omit<React.DetailedHTMLProps<React.HTMLAttributes<HTMLSpanElement>, HTMLSpanElement>, "ref"> & {
    ref?: ((instance: HTMLSpanElement | null) => void | React.DO_NOT_USE_OR_YOU_WILL_BE_FIRED_CALLBACK_REF_RETURN_VALUES[keyof React.DO_NOT_USE_OR_YOU_WILL_BE_FIRED_CALLBACK_REF_RETURN_VALUES]) | React.RefObject<HTMLSpanElement> | null | undefined;
  }, "ref"> & React.RefAttributes<HTMLSpanElement>>;
};
//#endregion
//#region src/mosaic/components/badge/badge.d.ts
type BadgeProps = MosaicComponentProps<'span'> & {
  color?: 'primary' | 'neutral' | 'warning' | 'negative' | 'positive';
};
/**
 * A small label that annotates adjacent content with a status or category. Renders a
 * `span` by default and forwards its ref; `color` sets the semantic color and `render`
 * swaps the element for polymorphism (e.g. a link).
 *
 * @example
 * // Default (primary)
 * <Badge>New</Badge>
 *
 * @example
 * // Semantic color
 * <Badge color='positive'>Active</Badge>
 * <Badge color='negative'>Failed</Badge>
 *
 * @example
 * // Polymorphic: render as a link
 * <Badge render={<a href='/billing' />}>Upgrade</Badge>
 */
declare const Badge: React.ForwardRefExoticComponent<Omit<BadgeProps, "ref"> & React.RefAttributes<HTMLSpanElement>>;
//#endregion
//#region src/mosaic/components/button/button.d.ts
interface ButtonProps extends MosaicElementProps<'button'> {
  color?: 'primary' | 'neutral' | 'negative';
  variant?: 'filled' | 'outline' | 'ghost' | 'link';
  size?: 'sm' | 'md' | 'lg';
  shape?: 'default' | 'square' | 'circle';
  fullWidth?: boolean;
  /**
   * Floors the hit area at the recommended touch target under a coarse pointer, without
   * changing the rendered size. Defaults to `true`. Pass `false` where buttons sit close
   * enough that the grown areas would overlap — a dense icon toolbar, a tight stack — since
   * the later sibling's area would otherwise cover the edge of the one before it. Has no
   * effect on `variant='link'`, which is text rather than a control.
   */
  touchTarget?: boolean;
  /**
   * Keeps the button in the tab order while `disabled`, so focus is not dropped when a button
   * disables itself mid-interaction — while a form submits, say — and the user keeps their place
   * on the page. The button is marked `aria-disabled` rather than `disabled`, and stays inert to
   * clicks and keyboard activation. It dims and reads as disabled either way.
   */
  focusableWhenDisabled?: boolean;
}
declare const Button: React.ForwardRefExoticComponent<Omit<ButtonProps, "ref"> & React.RefAttributes<HTMLButtonElement>>;
//#endregion
//#region src/mosaic/hooks/useSpinDelay.d.ts
interface SpinDelayOptions {
  /** Wait this long before showing the value, so quick actions never flash a spinner. */
  delay?: number;
  /** Once shown, keep the value up at least this long, so the spinner never flickers off. */
  minDuration?: number;
}
//#endregion
//#region src/mosaic/components/button/submit-button.d.ts
interface SubmitButtonProps extends ButtonProps {
  /**
   * Marks the action as running: the button goes inert, announces itself busy, and — once the
   * action outlasts a short delay — fades its label and centers a spinner over it. The label
   * stays mounted at zero opacity so the button keeps its width and the form around it does not
   * reflow while the action runs.
   */
  isPending?: boolean;
  /**
   * Accessible name for the pending indicator, announced alongside the button's own label. The
   * default is untranslated, so pass a localized string wherever the surrounding copy is.
   */
  pendingLabel?: string;
  /**
   * Tunes when the spinner appears and how long it stays. `delay` (default `300`) is how long the
   * action has to run before the spinner is drawn at all; `minDuration` (default `200`) is how
   * long it stays once drawn. Neither affects the pending state itself, which always applies
   * immediately. Set `delay: 0` for an action already known to be slow.
   */
  spinDelay?: SpinDelayOptions;
}
/**
 * A `Button` that submits its form, with a pending affordance. Takes every `Button` prop;
 * `type` defaults to `submit` and can still be overridden.
 *
 * While `isPending`, the button is inert but stays focusable and announced — see the
 * `isPending` prop for what that means for assistive tech.
 *
 * @example
 * <SubmitButton isPending={isSubmitting}>Save</SubmitButton>
 *
 * @example
 * // Pending state on a destructive action
 * <SubmitButton color='negative' isPending={isDeleting}>Delete</SubmitButton>
 */
declare const SubmitButton: React.ForwardRefExoticComponent<Omit<SubmitButtonProps, "ref"> & React.RefAttributes<HTMLButtonElement>>;
//#endregion
//#region src/mosaic/components/card/card.d.ts
type CardAlignment = 'start' | 'center';
type CardElevation = 'card' | 'flush' | 'overlay';
/** Props for `Card.Root`, including native `div` props and the Mosaic `render` escape hatch. */
interface CardProps extends MosaicComponentProps<'div'> {
  /** Alignment applied to `Card.Header`. @default 'start' */
  alignment?: CardAlignment;
  /** Surface treatment applied to the card. @default 'card' */
  elevation?: CardElevation;
  /**
   * Signs the foot of the card with "Secured by Clerk". An instance that has paid the branding off
   * carries none of it, so a connected surface passes `displayConfig.branded` here.
   *
   * @default true
   */
  renderBranding?: boolean;
}
/**
 * A styled surface composed through `Card.Root`, `Card.Header`, `Card.Content`, and `Card.Footer`.
 * Every part accepts the Mosaic `render` prop and forwards its ref.
 */
declare const Card: {
  Root: React.ForwardRefExoticComponent<Omit<CardProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Header: React.ForwardRefExoticComponent<Omit<MosaicComponentProps<"div">, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Content: React.ForwardRefExoticComponent<Omit<MosaicComponentProps<"div">, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Footer: React.ForwardRefExoticComponent<Omit<MosaicComponentProps<"div">, "ref"> & React.RefAttributes<HTMLDivElement>>;
};
//#endregion
//#region src/mosaic/components/field/field.d.ts
/** Props for a field container that associates exactly one form control. */
interface FieldRootProps extends MosaicComponentProps<'div'> {
  disabled?: boolean;
  required?: boolean;
  invalid?: boolean;
}
/** Props for a native field label. */
type FieldLabelProps = MosaicComponentProps<'label'>;
/** Props for supporting field text. */
type FieldDescriptionProps = MosaicComponentProps<'p'>;
/** Props for field validation text. */
type FieldErrorProps = MosaicComponentProps<'p'>;
/** Styled parts for composing an automatically associated single-control field. */
declare const Field: {
  Root: React.ForwardRefExoticComponent<Omit<FieldRootProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Label: React.ForwardRefExoticComponent<Omit<FieldLabelProps, "ref"> & React.RefAttributes<HTMLLabelElement>>;
  Description: React.ForwardRefExoticComponent<Omit<FieldDescriptionProps, "ref"> & React.RefAttributes<HTMLParagraphElement>>;
  Error: React.ForwardRefExoticComponent<Omit<FieldErrorProps, "ref"> & React.RefAttributes<HTMLParagraphElement>>;
};
//#endregion
//#region src/mosaic/components/typography.styles.d.ts
type TypographySize = 'xs' | 'sm' | 'base' | 'lg' | 'xl' | '2xl';
type TypographyColor = 'primary' | 'neutral' | 'warning' | 'negative' | 'positive';
//#endregion
//#region src/mosaic/components/heading/heading.d.ts
interface HeadingProps extends MosaicComponentProps<'h2'> {
  size?: TypographySize;
  color?: TypographyColor;
}
declare const HeadingContext: React.Context<Partial<HeadingProps> | null>;
/**
 * Themeable heading. Renders an `<h2>` by default, forwards refs, and supports
 * `size` and `color` variants. Pass `render` for a different heading level.
 */
declare const Heading: React.ForwardRefExoticComponent<Omit<HeadingProps, "ref"> & React.RefAttributes<HTMLHeadingElement>>;
//#endregion
//#region src/mosaic/components/icon/icon.d.ts
interface IconProps extends React.ComponentPropsWithRef<'svg'> {
  name: IconName;
  size?: 'sm' | 'md' | 'lg';
  placement?: 'inline-start' | 'inline-end';
}
/**
 * Renders a Mosaic icon by name. The glyph can be swapped per-name via the `icons` prop on
 * `MosaicProvider`; Mosaic's sizing is applied to the override exactly as it is to the built-in
 * glyph, so the two stay visually consistent.
 *
 * `placement` marks the icon as leading or trailing content within a container. It applies no
 * style here — it reflects an attribute the container styles against, so a `Button` can tighten
 * the padding on whichever side the icon sits. The values are the CSS logical directions, so they
 * follow the writing mode rather than naming a physical edge.
 */
declare const Icon: React.ForwardRefExoticComponent<Omit<IconProps, "ref"> & React.RefAttributes<SVGSVGElement>>;
//#endregion
//#region src/mosaic/icons/overrides.d.ts
/**
 * A replacement glyph for a named icon, authored as a React element (`<MyIcon />` or a raw
 * `<svg>…</svg>`). Mosaic injects its sizing/color `className`, `data-size`/`data-icon`, and any svg props
 * forwarded from the `<Icon>` call site into this element via `cloneElement`, so the override is
 * styled and targetable exactly like the built-in glyph. Author the element to accept those props
 * (a component should spread them onto its root svg; a raw svg receives them directly).
 *
 * An element rather than a render function so overrides serialize across the RSC server→client
 * boundary: `icons` can then be supplied from a Server Component. An inline function cannot cross
 * that boundary; an element (or a `'use client'` component reference) can.
 */
type MosaicIconOverride = React$1.ReactElement;
/** The `icons` prop: per-name glyph overrides, applied globally. */
type MosaicIconOverrides = Partial<Record<IconName, MosaicIconOverride>>;
//#endregion
//#region src/mosaic/components/input/input.d.ts
interface InputProps extends Omit<MosaicComponentProps<'input'>, 'size'> {
  size?: 'sm' | 'md' | 'lg';
}
declare const Input: React.ForwardRefExoticComponent<Omit<InputProps, "ref"> & React.RefAttributes<HTMLInputElement>>;
//#endregion
//#region src/mosaic/components/item/item.d.ts
/** The row's height and gap, and the width of the media column inside it. */
type Size = 'xs' | 'md';
type ItemProps = MosaicComponentProps<'div'> & {
  /**
   * Row height and gap. Also sizes a nested `Item.Media`, which reads this from
   * context rather than taking its own prop, so a row scales as one unit.
   *
   * @default 'md'
   */
  size?: Size;
};
/** Where the label sits in the row's hierarchy. */
type LabelVariant = 'primary' | 'secondary';
type ItemLabelProps = MosaicComponentProps<'div'> & {
  /**
   * `primary` names the row's subject (a person, an organization) and holds its
   * own strength. `secondary` is quieter and takes its color from the row rather
   * than setting one, so on an interactive row it brightens with the row on
   * hover instead of staying fixed. Use it where the text is the row itself
   * (`Add account`, `Sign out`) or a group heading, not a subject the row names.
   *
   * @default 'primary'
   */
  variant?: LabelVariant;
};
/**
 * Mosaic `Item` — a row for lists of accounts, organizations, and settings.
 * Composed via dot syntax: `Item.Root`, `Item.Media`, `Item.Content`,
 * `Item.Label`, `Item.Description`, `Item.Actions`, `Item.Group`,
 * `Item.Separator`. Every part takes a `render` prop and forwards a ref.
 *
 * `size` is set once on `Item.Root` and reaches `Item.Media` through context, so
 * a row scales as a unit rather than per part.
 */
declare const Item: {
  Root: React.ForwardRefExoticComponent<Omit<ItemProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Media: React.ForwardRefExoticComponent<Omit<MosaicComponentProps<"div">, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Content: React.ForwardRefExoticComponent<Omit<MosaicComponentProps<"div">, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Label: React.ForwardRefExoticComponent<Omit<ItemLabelProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Description: React.ForwardRefExoticComponent<Omit<MosaicComponentProps<"div">, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Actions: React.ForwardRefExoticComponent<Omit<MosaicComponentProps<"div">, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Group: React.ForwardRefExoticComponent<Omit<MosaicComponentProps<"div">, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Separator: React.ForwardRefExoticComponent<Omit<MosaicComponentProps<"hr">, "ref"> & React.RefAttributes<HTMLHRElement>>;
};
//#endregion
//#region src/mosaic/components/menu/menu.d.ts
type MenuTriggerProps = MosaicComponentProps<'button'>;
interface MenuContentProps extends MenuPopupProps {
  /** Container the menu portals into. Defaults to `document.body`. */
  portalRoot?: MenuPortalProps['root'];
}
interface MenuItemProps extends MenuItemProps$1 {
  /** Semantic color of the action. */
  color?: 'neutral' | 'negative';
}
/** A full-bleed divider between groups of items. */
declare function MenuSeparator({
  className,
  style,
  ...rest
}: MenuSeparatorProps): React.ReactElement;
declare const Menu: {
  Root: typeof Menu$1.Root;
  Trigger: React.ForwardRefExoticComponent<Omit<MenuTriggerProps, "ref"> & React.RefAttributes<HTMLButtonElement>>;
  Content: React.ForwardRefExoticComponent<Omit<MenuContentProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Item: React.ForwardRefExoticComponent<Omit<MenuItemProps, "ref"> & React.RefAttributes<HTMLButtonElement>>;
  Separator: typeof MenuSeparator;
};
//#endregion
//#region src/mosaic/components/scroll-area/scroll-area.styles.d.ts
declare const gutters: Readonly<{
  readonly auto: Readonly<{
    readonly scrollbarGutter: stylex.StyleXClassNameFor<"scrollbarGutter", "auto">;
  }>;
  readonly stable: Readonly<{
    readonly scrollbarGutter: stylex.StyleXClassNameFor<"scrollbarGutter", "stable">;
  }>;
}>;
type ScrollAreaGutter = keyof typeof gutters;
/**
 * The scroll surface, as StyleX atoms to spread onto an element you already render.
 *
 * There is no `<ScrollArea>` component: everything here is CSS, so a component would only add
 * a DOM node and an API to version. Put these on whatever already scrolls — an `Item.Group`,
 * a list, a panel body — and it keeps its own slot class, which stays the hook a theme
 * targets.
 *
 * ```tsx
 * <div {...stylex.props(scrollAreaRoot)}>
 *   <Item.Group {...stylex.props(...scrollAreaViewport())}>{rows}</Item.Group>
 * </div>
 * ```
 *
 * @param gutter - Whether the scrollbar's space is held open. `auto` (the default, and CSS's
 * own) takes it only while the content overflows. `stable` reserves it either way, which is
 * worth it when the content can change height **in place** — a filterable or paginated
 * collection — so crossing the overflow threshold doesn't shift the rows sideways. Neither
 * does anything on platforms that overlay their scrollbars.
 */
declare function scrollAreaViewport(gutter?: ScrollAreaGutter): readonly [Readonly<{
  readonly overscrollBehavior: stylex.StyleXClassNameFor<"overscrollBehavior", "contain">;
  readonly flexBasis: stylex.StyleXClassNameFor<"flexBasis", "auto">;
  readonly flexGrow: stylex.StyleXClassNameFor<"flexGrow", 1>;
  readonly flexShrink: stylex.StyleXClassNameFor<"flexShrink", 1>;
  readonly scrollPaddingBlockEnd: stylex.StyleXClassNameFor<"scrollPaddingBlockEnd", "1.5rem">;
  readonly scrollPaddingBlockStart: stylex.StyleXClassNameFor<"scrollPaddingBlockStart", "1.5rem">;
  readonly minHeight: stylex.StyleXClassNameFor<"minHeight", 0>;
  readonly overflowX: stylex.StyleXClassNameFor<"overflowX", "hidden">;
  readonly overflowY: stylex.StyleXClassNameFor<"overflowY", "auto">;
}>, Readonly<{
  readonly '--_cl-scrollbar-thumb-color': stylex.StyleXClassNameFor<"--_cl-scrollbar-thumb-color", `color-mix(in oklab, ${stylex.StyleXVar<"light-dark(oklch(0.5697 0.0246 279.94), oklch(0.6953 0.0261 285.7))">}, ${stylex.StyleXVar<"light-dark(oklch(1 0 0), oklch(0.205 0 0))">} 55%)` | `color-mix(in oklab, var(--cl-scrollbar-thumb), ${stylex.StyleXVar<"light-dark(oklch(1 0 0), oklch(0.205 0 0))">} 45%)` | null>;
  readonly transitionDuration: stylex.StyleXClassNameFor<"transitionDuration", "0.1s" | "0.15s" | null>;
  readonly transitionProperty: stylex.StyleXClassNameFor<"transitionProperty", "--_cl-scrollbar-thumb-color" | null>;
  readonly transitionTimingFunction: stylex.StyleXClassNameFor<"transitionTimingFunction", "linear" | null>;
}>, Readonly<{
  readonly '::-webkit-scrollbar': stylex.StyleXClassNameFor<"::-webkit-scrollbar", {
    readonly width: {
      readonly default: null;
      readonly '@media (pointer: fine)': stylex.StyleXVar<"8px">;
    };
  }>;
  readonly '::-webkit-scrollbar-thumb': stylex.StyleXClassNameFor<"::-webkit-scrollbar-thumb", {
    readonly borderColor: {
      readonly default: null;
      readonly '@media (pointer: fine)': "transparent";
    };
    readonly borderRadius: {
      readonly default: null;
      readonly '@media (pointer: fine)': stylex.StyleXVar<"calc(infinity * 1px)">;
    };
    readonly borderStyle: {
      readonly default: null;
      readonly '@media (pointer: fine)': "solid";
    };
    readonly borderWidth: {
      readonly default: null;
      readonly '@media (pointer: fine)': stylex.StyleXVar<"2px">;
    };
    readonly backgroundClip: {
      readonly default: null;
      readonly '@media (pointer: fine)': "content-box";
    };
    readonly backgroundColor: {
      readonly default: null;
      readonly '@media (pointer: fine)': {
        readonly default: stylex.StyleXVar<stylex.Types.Color<"transparent">>;
        readonly '@media (forced-colors: active)': "ButtonBorder";
      };
    };
  }>;
  readonly '::-webkit-scrollbar-thumb:active': stylex.StyleXClassNameFor<"::-webkit-scrollbar-thumb:active", {
    readonly '--_cl-scrollbar-thumb-color': {
      readonly default: null;
      readonly '@media (pointer: fine)': stylex.StyleXVar<`color-mix(in oklab, var(--cl-scrollbar-thumb), ${stylex.StyleXVar<"light-dark(oklch(0.145 0 0), oklch(0.985 0 0))">} 30%)`>;
    };
  }>;
  readonly '::-webkit-scrollbar-thumb:hover': stylex.StyleXClassNameFor<"::-webkit-scrollbar-thumb:hover", {
    readonly '--_cl-scrollbar-thumb-color': {
      readonly default: null;
      readonly '@media (pointer: fine)': stylex.StyleXVar<`color-mix(in oklab, var(--cl-scrollbar-thumb), ${stylex.StyleXVar<"light-dark(oklch(0.145 0 0), oklch(0.985 0 0))">} 15%)`>;
    };
  }>;
  readonly '::-webkit-scrollbar-track': stylex.StyleXClassNameFor<"::-webkit-scrollbar-track", {
    readonly backgroundColor: {
      readonly default: null;
      readonly '@media (pointer: fine)': "transparent";
    };
  }>;
}>, Readonly<{
  readonly maskImage: stylex.StyleXClassNameFor<"maskImage", string>;
  readonly maskPosition: stylex.StyleXClassNameFor<"maskPosition", "left top, right top">;
  readonly maskRepeat: stylex.StyleXClassNameFor<"maskRepeat", "no-repeat">;
  readonly maskSize: stylex.StyleXClassNameFor<"maskSize", "100% 100%, 0px 100%" | {
    readonly '@supports not (-moz-appearance: none)': `calc(100% - ${stylex.StyleXVar<"8px">}) 100%, ${stylex.StyleXVar<"8px">} 100%`;
  }>;
}>, Readonly<{
  readonly animationRange: stylex.StyleXClassNameFor<"animationRange", `0px ${stylex.StyleXVar<"1.5rem">}, calc(100% - ${stylex.StyleXVar<"1.5rem">}) 100%`>;
  readonly animationFillMode: stylex.StyleXClassNameFor<"animationFillMode", "both">;
  readonly animationName: stylex.StyleXClassNameFor<"animationName", `${string}, ${string}` | null>;
  readonly animationTimeline: stylex.StyleXClassNameFor<"animationTimeline", "scroll(self block), scroll(self block)">;
  readonly animationTimingFunction: stylex.StyleXClassNameFor<"animationTimingFunction", "linear">;
}>, Readonly<{
  readonly outline: stylex.StyleXClassNameFor<"outline", `2px solid ${stylex.StyleXVar<"light-dark(oklch(0.205 0 0), oklch(0.922 0 0))">}` | null>;
  readonly outlineOffset: stylex.StyleXClassNameFor<"outlineOffset", string | null>;
}>, Readonly<{
  readonly scrollbarGutter: stylex.StyleXClassNameFor<"scrollbarGutter", "auto">;
}> | Readonly<{
  readonly scrollbarGutter: stylex.StyleXClassNameFor<"scrollbarGutter", "stable">;
}>];
/**
 * The positioned ancestor. Only needed when something has to anchor against the scroll box —
 * an overlay replacing the default mask, or a future scrollbar. A scroll surface whose parent
 * is already positioned doesn't need it.
 */
declare const scrollAreaRoot: Readonly<{
  readonly display: stylex.StyleXClassNameFor<"display", "flex">;
  readonly flexDirection: stylex.StyleXClassNameFor<"flexDirection", "column">;
  readonly position: stylex.StyleXClassNameFor<"position", "relative">;
  readonly minHeight: stylex.StyleXClassNameFor<"minHeight", 0>;
}>;
//#endregion
//#region src/mosaic/components/scroll-area/scroll-area.vars.stylex.d.ts
declare const scrollAreaVars: stylex.VarGroup<Readonly<{
  '--cl-scroll-area-progress-start': stylex.Types.Num<0>;
  '--cl-scroll-area-progress-end': stylex.Types.Num<0>;
}>, symbol>;
//#endregion
//#region src/mosaic/components/section/section.d.ts
type SectionRootProps = Omit<MosaicComponentProps<'section'>, 'title'>;
type SectionTitleProps = Omit<HeadingProps, 'size'>;
type SectionGroupProps = MosaicComponentProps<'div'>;
type SectionRowProps = MosaicComponentProps<'div'>;
type SectionItemsProps = MosaicComponentProps<'div'>;
type SectionItemProps = MosaicComponentProps<'div'>;
type SectionMediaSize = 'sm' | 'md' | 'lg' | 'xl';
type SectionMediaProps = MosaicComponentProps<'div'> & {
  size?: SectionMediaSize;
};
type SectionContentProps = MosaicComponentProps<'div'>;
type SectionLabelProps = MosaicComponentProps<'div'>;
type SectionDescriptionProps = MosaicComponentProps<'div'>;
type SectionActionsProps = MosaicComponentProps<'div'>;
/**
 * A compound component that fixes section semantics, surface treatment, row grouping,
 * and item layout while leaving each item's content composable.
 */
declare const Section: {
  Root: React.ForwardRefExoticComponent<Omit<SectionRootProps, "ref"> & React.RefAttributes<HTMLElement>>;
  Title: React.ForwardRefExoticComponent<Omit<SectionTitleProps, "ref"> & React.RefAttributes<HTMLHeadingElement>>;
  Group: React.ForwardRefExoticComponent<Omit<SectionGroupProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Row: React.ForwardRefExoticComponent<Omit<SectionRowProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Items: React.ForwardRefExoticComponent<Omit<SectionItemsProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Item: React.ForwardRefExoticComponent<Omit<SectionItemProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Media: React.ForwardRefExoticComponent<Omit<SectionMediaProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Content: React.ForwardRefExoticComponent<Omit<SectionContentProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Label: React.ForwardRefExoticComponent<Omit<SectionLabelProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Description: React.ForwardRefExoticComponent<Omit<SectionDescriptionProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Actions: React.ForwardRefExoticComponent<Omit<SectionActionsProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
};
//#endregion
//#region src/mosaic/components/spinner/spinner.d.ts
type SpinnerProps = MosaicElementProps<'span'> & {
  size?: 'sm' | 'md';
};
/**
 * Indeterminate loading spinner. Decorative (`aria-hidden`) — pair it with a disabled control or an
 * `aria-busy` container so assistive tech is informed of the pending state.
 *
 * @example
 * // Default (md), sized to match a `md` Icon
 * <Spinner />
 *
 * @example
 * // Standing in for a `sm` Icon
 * <Spinner size='sm' />
 */
declare const Spinner: React.ForwardRefExoticComponent<Omit<SpinnerProps, "ref"> & React.RefAttributes<HTMLSpanElement>>;
//#endregion
//#region src/mosaic/components/text/text.d.ts
interface TextProps extends MosaicComponentProps<'p'> {
  size?: TypographySize;
  color?: TypographyColor;
}
declare const TextContext: React.Context<Partial<TextProps> | null>;
/**
 * Themeable body copy. Renders a `<p>` by default, forwards refs, and supports
 * `size` and `color` variants. Pass `render` for inline copy (`<span>`).
 */
declare const Text: React.ForwardRefExoticComponent<Omit<TextProps, "ref"> & React.RefAttributes<HTMLParagraphElement>>;
//#endregion
//#region src/mosaic/components/popover/popover.d.ts
type PopoverSize = 'sm' | 'md' | 'lg';
type PopoverRootProps = PopoverProps;
/**
 * The headless parts type their props (and the `render` callback's argument) against
 * the raw tag props, which carry the non-standard HTML `color` attribute typed
 * `string`. Re-typing them through `MosaicComponentProps` drops it, so a `render`
 * callback can spread straight into a Mosaic component whose own `color` is a narrow
 * variant union.
 */
type PopoverTriggerProps = MosaicComponentProps<'button'>;
type PopoverCloseProps = MosaicComponentProps<'button'>;
type PopoverTitleProps = MosaicComponentProps<'h2'>;
type PopoverDescriptionProps = MosaicComponentProps<'p'>;
interface PopoverPopupProps extends MosaicComponentProps<'div'> {
  /** Width of the floating box. */
  size?: PopoverSize;
  /**
   * Names the dialog for assistive technology. Required unless the contents render a
   * `Popover.Title`, which wires `aria-labelledby` instead.
   */
  'aria-label'?: string;
  /** Names the dialog from an existing element. Alternative to `aria-label`. */
  'aria-labelledby'?: string;
}
/**
 * Mosaic `Popover` — a floating box anchored to a trigger. Composed via dot
 * syntax: `Popover.Root`, `Popover.Trigger`, `Popover.Popup`, `Popover.Title`,
 * `Popover.Description`, `Popover.Close`.
 */
declare const Popover: {
  Root: typeof Popover$1.Root;
  Trigger: React.ForwardRefExoticComponent<Omit<PopoverTriggerProps, "ref"> & React.RefAttributes<HTMLButtonElement>>;
  Popup: React.ForwardRefExoticComponent<Omit<PopoverPopupProps, "ref"> & React.RefAttributes<HTMLDivElement>>;
  Title: React.ForwardRefExoticComponent<Omit<PopoverTitleProps, "ref"> & React.RefAttributes<HTMLHeadingElement>>;
  Description: React.ForwardRefExoticComponent<Omit<PopoverDescriptionProps, "ref"> & React.RefAttributes<HTMLParagraphElement>>;
  Close: React.ForwardRefExoticComponent<Omit<PopoverCloseProps, "ref"> & React.RefAttributes<HTMLButtonElement>>;
};
//#endregion
//#region src/mosaic/tokens.stylex.d.ts
declare const colorVars: stylex.VarGroup<Readonly<{
  readonly '--cl-color-primary': "light-dark(oklch(0.205 0 0), oklch(0.922 0 0))";
  readonly '--cl-color-primary-foreground': "light-dark(oklch(0.985 0 0), oklch(0.205 0 0))";
  readonly '--cl-color-primary-faded': "light-dark(oklch(0.9583 0.0214 291.74), oklch(0.3097 0.1008 285.05))";
  readonly '--cl-color-neutral': "light-dark(oklch(0.2928 0.0163 285.35), oklch(0.9854 0.0013 286.38))";
  readonly '--cl-color-neutral-foreground': "light-dark(oklch(0.24 0 0), oklch(0.96 0 0))";
  readonly '--cl-color-neutral-faded': "light-dark(oklch(0.5697 0.0246 279.94), oklch(0.6953 0.0261 285.7))";
  readonly '--cl-color-negative': "light-dark(oklch(0.577 0.245 27.325), oklch(0.637 0.237 25.331))";
  readonly '--cl-color-negative-foreground': "oklch(0.985 0 0)";
  readonly '--cl-color-negative-faded': "light-dark(oklch(0.9757 0.0118 17.36), oklch(0.255 0.0604 22.31))";
  readonly '--cl-color-positive': "light-dark(oklch(0.548 0.153 152.535), oklch(0.696 0.17 162.48))";
  readonly '--cl-color-positive-foreground': "oklch(0.985 0 0)";
  readonly '--cl-color-positive-faded': "light-dark(oklch(0.9859 0.0164 156.92), oklch(0.3297 0.052 152.31))";
  readonly '--cl-color-warning': "light-dark(oklch(0.646 0.222 41.116), oklch(0.75 0.183 55.934))";
  readonly '--cl-color-warning-foreground': "oklch(0.985 0 0)";
  readonly '--cl-color-warning-faded': "light-dark(oklch(0.9799 0.0147 70.89), oklch(0.2725 0.0547 55.7))";
  readonly '--cl-color-background': "light-dark(oklch(0.985 0 0), oklch(0.2686 0 0))";
  readonly '--cl-color-card': "light-dark(oklch(1 0 0), oklch(0.205 0 0))";
  readonly '--cl-color-card-foreground': "light-dark(oklch(0.145 0 0), oklch(0.985 0 0))";
  readonly '--cl-color-input': "light-dark(oklch(1 0 0), oklch(0.2046 0 0))";
  readonly '--cl-color-input-placeholder': "#a3a3a3";
  readonly '--cl-color-border': "light-dark(oklch(0.9219 0 0), oklch(0.3715 0 0))";
  readonly '--cl-color-border-faded': "light-dark(oklch(0.9702 0 0), oklch(0.296 0.0126 285.61))";
}>, symbol>;
declare const radiusVars: stylex.VarGroup<Readonly<{
  readonly '--cl-radius-none': "0rem";
  readonly '--cl-radius-sm': "0.25rem";
  readonly '--cl-radius-md': "0.375rem";
  readonly '--cl-radius-lg': "0.5rem";
  readonly '--cl-radius-xl': "0.75rem";
  readonly '--cl-radius-full': "calc(infinity * 1px)";
}>, symbol>;
declare const targetVars: stylex.VarGroup<Readonly<{
  readonly '--cl-target-coarse': "2.75rem";
}>, symbol>;
declare const scrollbarVars: stylex.VarGroup<Readonly<{
  readonly '--cl-scrollbar-width': "8px";
  readonly '--cl-scrollbar-thumb-inset': "2px";
  readonly '--cl-scrollbar-thumb': `color-mix(in oklab, ${stylex.StyleXVar<"light-dark(oklch(0.5697 0.0246 279.94), oklch(0.6953 0.0261 285.7))">}, ${stylex.StyleXVar<"light-dark(oklch(1 0 0), oklch(0.205 0 0))">} 55%)`;
  readonly '--cl-scrollbar-thumb-idle': `color-mix(in oklab, var(--cl-scrollbar-thumb), ${stylex.StyleXVar<"light-dark(oklch(1 0 0), oklch(0.205 0 0))">} 45%)`;
  readonly '--cl-scrollbar-thumb-hover': `color-mix(in oklab, var(--cl-scrollbar-thumb), ${stylex.StyleXVar<"light-dark(oklch(0.145 0 0), oklch(0.985 0 0))">} 15%)`;
  readonly '--cl-scrollbar-thumb-active': `color-mix(in oklab, var(--cl-scrollbar-thumb), ${stylex.StyleXVar<"light-dark(oklch(0.145 0 0), oklch(0.985 0 0))">} 30%)`;
}>, symbol>;
declare const scrollFadeVars: stylex.VarGroup<Readonly<{
  readonly '--cl-scroll-fade-size': "1.5rem";
  readonly '--cl-scroll-fade-range': "1.5rem";
}>, symbol>;
declare const spacingVars: stylex.VarGroup<Readonly<{
  readonly '--cl-spacing': "0.25rem";
}>, symbol>;
declare const space: stylex.VarGroup<Readonly<{
  '0': string;
  '0.5': string;
  '1': string;
  '1.5': string;
  '2': string;
  '2.5': string;
  '3': string;
  '3.5': string;
  '4': string;
  '4.5': string;
  '5': string;
  '5.5': string;
  '6': string;
  '6.5': string;
  '7': string;
  '7.5': string;
  '8': string;
  '8.5': string;
  '9': string;
  '9.5': string;
  '10': string;
  '10.5': string;
  '11': string;
  '11.5': string;
  '12': string;
  '12.5': string;
  '13': string;
  '13.5': string;
  '14': string;
  '14.5': string;
  '15': string;
  '15.5': string;
  '16': string;
  '16.5': string;
  '17': string;
  '17.5': string;
  '18': string;
  '18.5': string;
  '19': string;
  '19.5': string;
  '20': string;
  '20.5': string;
  '21': string;
  '21.5': string;
  '22': string;
  '22.5': string;
  '23': string;
  '23.5': string;
  '24': string;
  '24.5': string;
  '25': string;
  '25.5': string;
  '26': string;
  '26.5': string;
  '27': string;
  '27.5': string;
  '28': string;
  '28.5': string;
  '29': string;
  '29.5': string;
  '30': string;
  '30.5': string;
  '31': string;
  '31.5': string;
  '32': string;
  '32.5': string;
  '33': string;
  '33.5': string;
  '34': string;
  '34.5': string;
  '35': string;
  '35.5': string;
  '36': string;
  '36.5': string;
  '37': string;
  '37.5': string;
  '38': string;
  '38.5': string;
  '39': string;
  '39.5': string;
  '40': string;
}>, symbol>;
declare const typeScaleVars: stylex.VarGroup<Readonly<{
  readonly '--cl-text-xs-size': "0.75rem";
  readonly '--cl-text-xs-leading': "calc(1 / 0.75)";
  readonly '--cl-text-sm-size': "0.875rem";
  readonly '--cl-text-sm-leading': "calc(1.25 / 0.875)";
  readonly '--cl-text-base-size': "1rem";
  readonly '--cl-text-base-leading': "calc(1.5 / 1)";
  readonly '--cl-text-lg-size': "1.125rem";
  readonly '--cl-text-lg-leading': "calc(1.75 / 1.125)";
  readonly '--cl-text-xl-size': "1.25rem";
  readonly '--cl-text-xl-leading': "calc(1.75 / 1.25)";
  readonly '--cl-text-2xl-size': "1.5rem";
  readonly '--cl-text-2xl-leading': "calc(2 / 1.5)";
}>, symbol>;
declare const fontFamilyVars: stylex.VarGroup<Readonly<{
  readonly '--cl-font-family-sans': "inherit";
}>, symbol>;
declare const fontWeightVars: stylex.VarGroup<Readonly<{
  readonly '--cl-font-normal': "400";
  readonly '--cl-font-medium': "500";
  readonly '--cl-font-semibold': "600";
  readonly '--cl-font-bold': "700";
}>, symbol>;
declare const durationVars: stylex.VarGroup<Readonly<{
  readonly '--cl-duration-instant': "0s";
  readonly '--cl-duration-fast': "0.1s";
  readonly '--cl-duration-base': "0.15s";
  readonly '--cl-duration-slow': "0.25s";
  readonly '--cl-duration-slower': "0.35s";
}>, symbol>;
declare const easingVars: stylex.VarGroup<Readonly<{
  readonly '--cl-ease-default': "cubic-bezier(0.175, 0.885, 0.32, 1.1)";
  readonly '--cl-ease-enter': "cubic-bezier(0, 0, 0.2, 1)";
  readonly '--cl-ease-exit': "cubic-bezier(0.55, 0.085, 0.68, 0.53)";
}>, symbol>;
//#endregion
//#region src/mosaic/styles/index.d.ts
type ColorVarName = keyof typeof colorVars;
type DurationVarName = keyof typeof durationVars;
type EasingVarName = keyof typeof easingVars;
type FontFamilyVarName = keyof typeof fontFamilyVars;
type FontWeightVarName = keyof typeof fontWeightVars;
type RadiusVarName = keyof typeof radiusVars;
type ScrollbarVarName = keyof typeof scrollbarVars;
type ScrollFadeVarName = keyof typeof scrollFadeVars;
type SpacingVarName = keyof typeof spacingVars;
type TargetVarName = keyof typeof targetVars;
type TypeScaleVarName = keyof typeof typeScaleVars;
//#endregion
export { AlertDialog, type AlertDialogActionsProps, type AlertDialogBackdropProps, type AlertDialogCloseProps, type AlertDialogConfirmProps, type AlertDialogDescriptionProps, type AlertDialogPopupProps, type AlertDialogProps, type AlertDialogRootProps, type AlertDialogTitleProps, type AlertDialogTriggerProps, type AlertDialogViewportProps, Avatar, type AvatarFallbackProps, type AvatarIconProps, type AvatarImageProps, type AvatarProps, Badge, type BadgeProps, Button, type ButtonProps, Card, type CardProps, ColorVarName, type ConfirmHandle, type ConfirmOptions, Dialog, type DialogBackdropProps, type DialogCloseButtonProps, type DialogCloseProps, type DialogDescriptionProps, type DialogPopupProps, type DialogProps, type DialogRootProps, type DialogSize, type DialogTitleProps, type DialogTriggerProps, type DialogViewportProps, DurationVarName, EasingVarName, Field, type FieldDescriptionProps, type FieldErrorProps, type FieldLabelProps, type FieldRootProps, FontFamilyVarName, FontWeightVarName, Heading, HeadingContext, type HeadingProps, Icon, type IconProps, Input, type InputProps, Item, type ItemProps, Menu, type MenuContentProps, type MenuItemProps, type MenuProps, type MenuSeparatorProps, type MenuTriggerProps, type MosaicComponentProps, type MosaicElementProps, type MosaicIconOverride, type MosaicIconOverrides, Popover, type PopoverCloseProps, type PopoverDescriptionProps, type PopoverPopupProps, type PopoverRootProps, type PopoverSize, type PopoverTitleProps, type PopoverTriggerProps, ProfilePage, type ProfilePageContentProps, type ProfilePageItem, type ProfilePagePanelProps, type ProfilePageRootProps, type ProfilePageSidebarProps, RadiusVarName, type ScrollAreaGutter, ScrollFadeVarName, ScrollbarVarName, Section, type SectionActionsProps, type SectionContentProps, type SectionDescriptionProps, type SectionGroupProps, type SectionItemProps, type SectionItemsProps, type SectionLabelProps, type SectionMediaProps, type SectionMediaSize, type SectionRootProps, type SectionRowProps, type SectionTitleProps, SpacingVarName, type SpinDelayOptions, Spinner, type SpinnerProps, SubmitButton, type SubmitButtonProps, TargetVarName, Text, TextContext, type TextProps, TypeScaleVarName, type UseConfirmedCloseOptions, colorVars, createConfirmHandle, durationVars, easingVars, fontFamilyVars, fontWeightVars, mergeStyleProps, radiusVars, scrollAreaRoot, scrollAreaVars, scrollAreaViewport, scrollFadeVars, scrollbarVars, space, spacingVars, targetVars, themeProps, typeScaleVars, useConfirmedClose };
//# sourceMappingURL=index.d.ts.map